module Rango
  class Bundle
    desc 'install', 'install all the runtime dependencies'
    def install(name)
    end

    desc 'list', 'list all the runtime dependencies'
    def list(name)
    end
  end
end
