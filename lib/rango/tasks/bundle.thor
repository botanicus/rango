# coding=utf-8

class Rango
  class Bundle < Thor
    # @since 0.0.1
    desc 'install', 'install all the runtime dependencies'
    def install(name)
    end

    # @since 0.0.1
    desc 'list', 'list all the runtime dependencies'
    def list(name)
    end
  end
end
