h1. Controllers

class Posts < Rango::Controller
  before :authenticate

  def authenticate
    raise SkipFilter if session.user.authorized?
    # ...
  end

  def show(slug)
    @post = Post.get(slug)
    display @post
  end
end